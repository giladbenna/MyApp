import 'package:flutter/material.dart';

class AnimalMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Animal Menu'),
      ),
      body: Center(
        child: Text('Welcome to the Animal Menu'),
      ),
    );
  }
}
